// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: ping.proto

package pingconnect

import (
	context "context"
	errors "errors"
	http "net/http"
	ping "rpc/internal/ping/pingpb"
	strings "strings"

	connect "connectrpc.com/connect"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// PingServiceName is the fully-qualified name of the PingService service.
	PingServiceName = "ping.PingService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// PingServicePingProcedure is the fully-qualified name of the PingService's Ping RPC.
	PingServicePingProcedure = "/ping.PingService/Ping"
)

// PingServiceClient is a client for the ping.PingService service.
type PingServiceClient interface {
	Ping(context.Context, *connect.Request[ping.PingReq]) (*connect.Response[ping.PingRes], error)
}

// NewPingServiceClient constructs a client for the ping.PingService service. By default, it uses
// the Connect protocol with the binary Protobuf Codec, asks for gzipped responses, and sends
// uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the connect.WithGRPC() or
// connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewPingServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) PingServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	pingServiceMethods := ping.File_ping_proto.Services().ByName("PingService").Methods()
	return &pingServiceClient{
		ping: connect.NewClient[ping.PingReq, ping.PingRes](
			httpClient,
			baseURL+PingServicePingProcedure,
			connect.WithSchema(pingServiceMethods.ByName("Ping")),
			connect.WithClientOptions(opts...),
		),
	}
}

// pingServiceClient implements PingServiceClient.
type pingServiceClient struct {
	ping *connect.Client[ping.PingReq, ping.PingRes]
}

// Ping calls ping.PingService.Ping.
func (c *pingServiceClient) Ping(ctx context.Context, req *connect.Request[ping.PingReq]) (*connect.Response[ping.PingRes], error) {
	return c.ping.CallUnary(ctx, req)
}

// PingServiceHandler is an implementation of the ping.PingService service.
type PingServiceHandler interface {
	Ping(context.Context, *connect.Request[ping.PingReq]) (*connect.Response[ping.PingRes], error)
}

// NewPingServiceHandler builds an HTTP handler from the service implementation. It returns the path
// on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewPingServiceHandler(svc PingServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	pingServiceMethods := ping.File_ping_proto.Services().ByName("PingService").Methods()
	pingServicePingHandler := connect.NewUnaryHandler(
		PingServicePingProcedure,
		svc.Ping,
		connect.WithSchema(pingServiceMethods.ByName("Ping")),
		connect.WithHandlerOptions(opts...),
	)
	return "/ping.PingService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case PingServicePingProcedure:
			pingServicePingHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedPingServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedPingServiceHandler struct{}

func (UnimplementedPingServiceHandler) Ping(context.Context, *connect.Request[ping.PingReq]) (*connect.Response[ping.PingRes], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("ping.PingService.Ping is not implemented"))
}
